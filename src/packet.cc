#include "packet.h"

int setup_packet( const ScanRequest& scan_request, IP_packet& ip_packet ) {

   return 0;
}

int parse_packet( const PacketBuffer&, const ScanRequest& scan_request, 
                  IP_packet& ip_packet ) {

   return 0;
}